package com.filestech.appmanager.ui.theme

import androidx.compose.material3.Typography

/**
 * Typography for App Manager Tech.
 *
 * Phase I: Material 3 default typography (Roboto via system fonts).
 * Custom typeface (e.g. Google Fonts) can be introduced in Phase II without
 * breaking any existing screen — just replace the [Typography] instance here.
 */
val AppTypography = Typography(
    // All text styles inherit Material 3 defaults.
    // Override individual styles as needed once brand guidelines are finalised.
)
